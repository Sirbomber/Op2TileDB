void PlaceTile(int _atX, int _atY, int _tileType)
{
	// set tile at map coordinates
	// if Y != bottom edge of map, check tileDB and set celltype to whatever we've defined this tile's celltype to be (need to update tileDB to include celltype info)
	// else celltype = I1
}